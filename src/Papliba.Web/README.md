# Papliba Application

The Next.js + React + TypeScript interface for the local Papliba workflow
builder. The separate product website lives at
https://github.com/sunnybharne/papliba-site.

## Commands

```bash
npm install
npm run dev
npm run build
```

The development application opens at `http://127.0.0.1:3000/`.
